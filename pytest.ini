[pytest]
# Add the project root to sys.path so 'src' is importable as a package.
# Without this, pytest may add 'src/' directly, making two_factor_lifted_heston
# a top-level module and breaking relative imports in src/lifted_heston/.
pythonpath = .
testpaths = tests
